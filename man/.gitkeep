This directory is populated automatically by running `devtools::document()`
(which reads the roxygen2 comments in R/*.R and writes .Rd files here).
Run it once after cloning, before `devtools::check()` or `R CMD build`.
